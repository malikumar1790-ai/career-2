@@ .. @@
 # Nexariza AI - Environment Variables Template
 # Copy this file to .env.local and fill in your actual values

-# SMTP Configuration for Nexariza AI
+# SMTP Configuration (Required for contact form emails)
 SMTP_HOST=smtpout.secureserver.net
 SMTP_PORT=587
 SMTP_USER=contact@nexariza.com
 SMTP_PASS=Nexariza@Ahmad1122

-# Alternative SMTP_PORT for SSL (if needed)
-# SMTP_PORT=465
+# Admin Configuration (Where admin notifications are sent)
+ADMIN_EMAIL=contact@nexariza.com
+CONTACT_EMAIL=contact@nexariza.com

-# General Configuration
-CONTACT_EMAIL=contact@nexariza.com
-NEXT_PUBLIC_SITE_URL=https://www.nexariza.com
+# Supabase Configuration (Required for database storage)
+VITE_SUPABASE_URL=https://your-project.supabase.co
+VITE_SUPABASE_ANON_KEY=your-anon-key-here
+
+# Alternative environment variable names (for compatibility)
+NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
+NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

-# Optional: Development mode flag
+# Site Configuration
+VITE_SITE_URL=https://www.nexariza.com
 NODE_ENV=production

-# Optional: Disable Service Worker in development
-VITE_DISABLE_SERVICE_WORKER=true
-
-# Optional: Database Configuration (for storing submissions)
-# DATABASE_URL=your_database_connection_string
-
-# Optional: Analytics
-# GOOGLE_ANALYTICS_ID=your_ga_id
-# HOTJAR_ID=your_hotjar_id
+# Optional: Alternative SMTP port for SSL
+# SMTP_PORT=465 (use this if 587 doesn't work)